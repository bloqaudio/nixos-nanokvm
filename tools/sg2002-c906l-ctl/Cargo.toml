[package]
name = "sg2002-c906l-ctl"
version = "0.1.0"
edition = "2024"
license = "MIT"
publish = false

[dependencies]
rustix = { version = "=1.1.4", default-features = false, features = ["event", "fs", "std"] }

[profile.release]
codegen-units = 1
lto = "thin"
overflow-checks = true
strip = true
